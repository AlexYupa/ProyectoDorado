import { FilterpersonaPipe } from './filterpersona.pipe';

describe('FilterpersonaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpersonaPipe();
    expect(pipe).toBeTruthy();
  });
});
