import { FillPacienteByCedulaPipe } from './fill-paciente-by-cedula.pipe';

describe('FillPacienteByCedulaPipe', () => {
  it('create an instance', () => {
    const pipe = new FillPacienteByCedulaPipe();
    expect(pipe).toBeTruthy();
  });
});
