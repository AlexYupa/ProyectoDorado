import { FilterconsultaPipe } from './filterconsulta.pipe';

describe('FilterconsultaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterconsultaPipe();
    expect(pipe).toBeTruthy();
  });
});
